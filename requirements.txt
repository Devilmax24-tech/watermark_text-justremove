# ─ Web Framework ─────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.6.0

# ─ PyTorch (CPU-optimized, smaller packages) ──────────────
# NOTE: These are heavy (~2GB total). Vercel cannot deploy.
# For Vercel: Use Render instead (see VERCEL_DEPLOYMENT_GUIDE.md)
torch==2.1.0
torchvision==0.16.0

# ─ Image Processing ──────────────────────────────────────
opencv-python-headless==4.9.0.80
Pillow==10.2.0
numpy==1.24.3
scipy==1.13.0
scikit-image==0.22.0

# ─ Configuration & Utils ─────────────────────────────────
omegaconf==2.3.5
PyYAML==6.0.1
python-dotenv==1.0.0
kornia==0.7.2

# ─ Inpainting Model ──────────────────────────────────────
simple-lama-inpainting==0.1.0

# ─ Performance Optimization ─────────────────────────────
uvloop==0.19.0

